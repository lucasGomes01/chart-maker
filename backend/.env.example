# ==========================================================
#  BACKEND — Template de Variáveis de Ambiente
#  Copie para .env e preencha com suas credenciais reais.
# ==========================================================

# OpenAI
OpenAI__ApiKey=

# Supabase (chave secreta — só no backend)
Supabase__SecretKey=

# Banco de dados PostgreSQL (Docker Compose padrão)
ConnectionStrings__Database=Host=products.database;Port=5432;Database=chartmaker;Username=postgres;Password=postgres
